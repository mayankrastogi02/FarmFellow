const ProfileCard = (userData) => {
    // const { age, email, first_name, last_name, gender, height, phone, profile_picture, weight } = userData

    console.log(userData)
    return (
        <div className="bg-darkBackground bg-opacity-5">
            <div>
                {/* <p>{first_name} {last_name}</p> */}

            </div>
        </div>
    )
}

export default ProfileCard